@model List<string>

@{
    ViewBag.Title = "Select Database";
}

<div style="text-align:right; margin-bottom:10px;">
    <button type="button" onclick="history.back()"
            style="padding:6px 14px; background:#007bff; color:white; border:none; border-radius:4px; cursor:pointer;">
        ← Back
    </button>
</div>

<h2>Select Database</h2>

<form method="get" action="/AdminEmployeeJobCardReport/Parameter">
    <input type="hidden" name="serverId" value="@Session["JobCardServerId"]" />

    <div style="margin-bottom:15px;">
        <label>Database</label><br />
        <select name="databaseName" required style="min-width:260px; padding:6px;">
            <option value="">-- Select Database --</option>
            @if (Model != null && Model.Count > 0)
            {
                foreach (var db in Model)
                {
                    <option value="@db">@db</option>
                }
            }
        </select>
    </div>

    <button type="submit">Next</button>
</form>